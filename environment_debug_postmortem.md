# 环境排查复盘

## 背景

在验证新增本体实体是否能被真实抽取时，最初没有直接跑通模型调用链路，随后错误地走到了“尝试新建本地虚拟环境”的分支。这个过程虽然没有破坏项目代码，但属于一次不必要的环境排查弯路，需要明确记录，避免后续重复犯同样的问题。

## 现象

- 直接使用当前会话默认的 `python3` 执行抽取时，最初判断为缺少 `openai` 依赖。
- 同时在当前 shell 环境中未直接看到 `DEEPSEEK_API_KEY`。
- 因此一度推断需要重新创建 `.venv` 并补装依赖。

## 根因

实际根因不是“项目环境缺失”，而是“使用了错误的解释器和错误的环境变量观察路径”：

1. 当前会话默认使用的是系统解释器 `/usr/bin/python3`，不是项目已有虚拟环境。
2. 系统 Python 的用户 site 目录当前不可写，导致“直接补依赖到当前解释器”这条路不可取。
3. 项目仓库下其实已经存在可用的虚拟环境：
   - `/root/remote-test/.venv/bin/python`
4. 该虚拟环境中已经安装好了模型调用所需依赖：
   - `openai==2.37.0`
5. `DEEPSEEK_API_KEY` 也并未缺失，而是通过 hermes 环境文件注入：
   - `/root/.hermes/.env`

## 错误动作

本次不应执行如下方向的操作：

- 在未确认仓库现有 `.venv` 是否可用前，尝试重新创建虚拟环境
- 仅凭当前 shell 中未直接读到环境变量，就判断 key 不存在
- 先做“重建环境”，后做“确认现有环境”

## 正确做法

后续在本项目中涉及模型调用、后端脚本、解析验证时，优先按下面顺序检查：

1. 先确认项目虚拟环境是否存在
   - ` /root/remote-test/.venv/bin/python `
2. 再确认该解释器是否已安装核心依赖
   - `openai`
   - `flask`
   - `pyyaml`
3. 再确认运行时环境变量来源
   - 优先检查 `/root/.hermes/.env`
4. 验证时统一使用：
   - `zsh -lc 'set -a; source /root/.hermes/.env >/dev/null 2>&1; set +a; /root/remote-test/.venv/bin/python ...'`

## 本次确认结论

- 可用解释器：
  - `/root/remote-test/.venv/bin/python`
- 可用 key 注入来源：
  - `/root/.hermes/.env`
- 不需要重新创建虚拟环境
- 不需要往系统 Python 强行补装依赖

## 后续规范

后续如果需要做抽取验证、重跑测试案件、调用后端解析链，统一遵守以下规范：

- 默认不用系统 `python3`
- 默认优先复用项目 `.venv`
- 默认通过 hermes 环境文件注入 key
- 只有在 `.venv` 明确损坏或缺失时，才考虑重建环境

## 本次影响

- 影响范围：仅限排查过程，未引入业务代码回退或数据破坏
- 已处理事项：
  - 停止误开的虚拟环境重建命令
  - 改为复用现有 `.venv`
  - 成功完成真实抽取验证
  - 已将最新解析结果写回 `visualization/data/test_data.json`

## 一句话结论

这次问题不是“环境不存在”，而是“没有第一时间切到项目现成环境”。后续本项目的标准运行入口应视为：

`/root/remote-test/.venv/bin/python` + `/root/.hermes/.env`
