# Tarot Workspace

这是塔罗产品的独立工作目录。

当前已初始化：

1. `resources/image_packs/`
2. `resources/meaning_packs/`
3. `resources/spread_packs/`
4. `resources/source_manifests/`

建议下一步：

1. 导入第一套 RWS 公版图片
2. 补全 22 张大阿尔卡那 `card.yaml`
3. 建立三牌阵与基础位置规则
4. 开始构建 `reading_context`
