# 每日代码评审报告 — 2026-04-29

> 评审人：大剑（Hermes Agent）
> 评审日期：2026-04-30
> 评审范围：remote-test 仓库最近 24 小时（2026-04-29 08:00 ~ 2026-04-30 08:00）内的全部代码变更
> 提交数量：37 次提交（18 次实质性代码变更 + 19 次评审对话/报告更新）

---

## 一、变更概览

### 1.1 提交统计

| 类别 | 数量 | 占比 |
|------|------|------|
| 本体论/Schema 升级 | 5 | 27.8% |
| 提取/解析脚本 | 5 | 27.8% |
| 评估/测试 | 3 | 16.7% |
| 基础设施/API | 3 | 16.7% |
| 文档/报告 | 2 | 11.1% |

### 1.2 核心变更文件（85 个文件，+29,472/-132 行）

**新增模块：**
- `ontology/schemas/legal_ontology_v2.yaml` — 生产级法律本体 Schema v2.0
- `ontology/pydantic_models.py` — Pydantic v2 运行时校验模型
- `ontology/schemas/sync_ontology_zh.py` — 中英双语 Schema 同步脚本
- `scripts/text_compressor.py` — 长文本结构化压缩器
- `scripts/parse_guiding_cases_llm_v2.py` — 长文本优化版 LLM 解析器
- `scripts/extract_legal_provisions_regex.py` — 法条正则提取器 v2.2
- `scripts/legal_provision_extractor.py` — 规则法条补提器
- `scripts/iterative_parse_eval.py` — 迭代式解析与本体对齐评估
- `scripts/evaluate_regex.py` — 正则提取效果评估
- `extraction/llm_extractors/guiding_case_extractor.py` — 生产级 LLM 提取器
- `kg_core/graph_store/neo4j_hot_client.py` — Neo4j 热层客户端
- `kg_core/case_reasoning/sentencing_predictor.py` — 判决推理预测器
- `api/routers/query.py` — 类案查询与判决推理 API
- `webui/backend/kg_builder.py` — Cytoscape.js 图谱构建器
- `docker-compose.yml` — 全栈基础设施编排
- `data/reference/case_type_to_provisions.json` — 案由→典型法条映射表

**重大修改：**
- `README.md` — 从单句升级为完整项目文档（+137 行）
- `scripts/prompts/guiding_case_extraction.txt` — 提示词对齐本体论 v2.2
- `scripts/prompts/guiding_case_ontology_aligned.txt` — 本体对齐版提示词
- `scripts/daily_review.py` — 每 2 小时对话评审模式

---

## 二、质量评估

### 2.1 总体评分

| 维度 | 评分 | 权重 | 加权得分 |
|------|------|------|----------|
| 完整性 | 88/100 | 25% | 22.0 |
| 一致性 | 85/100 | 25% | 21.3 |
| 准确性 | 82/100 | 25% | 20.5 |
| 可维护性 | 80/100 | 15% | 12.0 |
| 安全性 | 75/100 | 10% | 7.5 |
| **综合评分** | — | **100%** | **83.3/100** |

### 2.2 评分说明

**完整性（88/100）**
- ✅ 优点：本体论从 v1 跃升至 v2.2，新增 LegalProvisionVersion、SentencingStandard、CaseSummary 等 3 个核心实体；提取链路覆盖 LLM → 正则 → 案由映射三层补提；评估报告详尽（20 样本 + 10 样本验证）
- ❌ 不足：证据实体（Evidence）虽已定义但未在提取流程中落地；脱敏引擎仅停留在方案阶段未实现；WebUI 前端未连接真实 API（mock 数据）

**一致性（85/100）**
- ✅ 优点：Schema/YAML/Pydantic/Prompt 四者保持同步；新增 `sync_ontology_zh.py` 脚本确保中英双语 Schema 一致性；枚举值在全链路统一
- ❌ 不足：部分旧脚本（`parse_guiding_cases.py` v1）与新本体论未完全对齐；`legal_ontology_v2.zh.yaml` 与 `.yaml` 存在字段顺序差异；docker-compose.yml 中硬编码密码与 config.yaml 中引用方式不一致

**准确性（82/100）**
- ✅ 优点：正则提取 v2.2 修复了致命 typo（笭→第），正确率 100%（已提取部分）；20 样本评估报告详尽，根因分析到位
- ❌ 不足：LLM 提取在长文本场景仍有幻觉（ID 3692、3538 的 judgment_result/guiding_points 为空）；法条填充率仅 30-40%，大量案件无显式法条引用；案号提取率 60%，调解/仲裁/司法救助案件案号缺失

**可维护性（80/100）**
- ✅ 优点：模块化设计清晰（extraction/kg_core/api/webui 分层）；Makefile 提供统一命令入口；config.yaml 集中管理配置
- ❌ 不足：部分文件缺少类型注解（`webui/frontend/app.js` 等前端代码）；错误处理采用裸 `except Exception` 模式，不利于调试；缺少单元测试（tests/ 目录仅 `__init__.py`）

**安全性（75/100）**
- ⚠️ 严重：docker-compose.yml 中明文硬编码数据库密码（`NEO4J_AUTH=***`、`POSTGRES_PASSWORD=***`、`MINIO_ROOT_PASSWORD=***`）
- ⚠️ 中等：`neo4j_hot_client.py` 中 `NEO4J_PASSWORD=os.getenv(..., "legalkg2026")` 存在默认硬编码密码回退
- ⚠️ 中等：API 端点未实现认证/鉴权（`api/routers/query.py` 所有接口公开可访问）
- ✅ 优点：Pydantic 模型对敏感字段（credit_code、phone、id_number）有正则校验；脱敏规则在文档中有明确规划

---

## 三、具体问题列表

### 🔴 P0 严重问题

| # | 位置 | 问题 | 影响 | 修复建议 |
|---|------|------|------|----------|
| 1 | `docker-compose.yml:19,33,43` | 数据库密码明文硬编码 | 生产环境部署时凭证泄露风险极高 | 改用 `.env` 文件 + `env_file` 引用，或 Docker Secrets |
| 2 | `kg_core/graph_store/neo4j_hot_client.py:14` | Neo4j 默认密码硬编码回退 | 即使环境变量未设置也会使用弱密码 | 移除默认值，强制要求环境变量，启动时校验 |
| 3 | `api/routers/query.py` | 所有 API 端点无认证鉴权 | 敏感司法数据接口公开暴露 | 增加 API Key / JWT 认证中间件，至少实现 IP 白名单 |

### 🟡 P1 中等问题

| # | 位置 | 问题 | 影响 | 修复建议 |
|---|------|------|------|----------|
| 4 | `scripts/parse_guiding_cases_llm_v2.py:71-78` | 裸 `except Exception` 捕获所有异常 | 隐藏真实错误，调试困难 | 细分为 `JSONDecodeError`、`TimeoutError`、`APIError` 等具体异常 |
| 5 | `extraction/llm_extractors/guiding_case_extractor.py:147-157` | OpenAI/DeepSeek/Kimi 客户端重复代码 | 新增提供商时需修改多处，违反 DRY | 抽象为统一的 `LLMClient` 工厂类 |
| 6 | `ontology/pydantic_models.py:1` | 缺少 `__version__` 和变更日志 | 无法追踪模型版本兼容性 | 增加 `__version__ = "2.2.0"`，并创建 `CHANGELOG.md` |
| 7 | `scripts/text_compressor.py:118-122` | `unnumbered_patterns` 中的正则可能过度匹配 | 争议焦点提取可能包含非争议内容 | 增加长度过滤（5-60 字）和否定词排除（"不认为"、"并非"） |
| 8 | `data/reference/case_type_to_provisions.json` | 仅覆盖 17 个案由类型 | 大量案由无法映射，法条补提率低 | 扩展至最高法案由规定全部 4 级案由体系 |
| 9 | `scripts/daily_review.py:15` | 硬编码仓库路径 `/root/.hermes/hermes-agent/remote-test` | 在其他环境无法运行 | 改用 `Path(__file__).resolve().parents[2]` 动态推导 |

### 🟢 P2 轻微问题

| # | 位置 | 问题 | 影响 | 修复建议 |
|---|------|------|------|----------|
| 10 | `webui/frontend/app.js` | 前端代码无 JSDoc/类型注解 | 维护成本高 | 增加 JSDoc 注释或迁移到 TypeScript |
| 11 | `tests/__init__.py` | 测试目录为空 | 无法自动化验证回归 | 为核心模块（正则提取、文本压缩、Pydantic 模型）添加 pytest 用例 |
| 12 | `scripts/enrich_provisions.py` | `--use-mapping` 默认 false | 法条填充率低 | 在验证映射准确性后，将默认值改为 true |
| 13 | `ontology/schemas/legal_ontology_v2.yaml` | 缺少 `Evidence` 与 `Fact` 的关联关系示例 | 图谱构建时可能遗漏证据-事实边 | 在 YAML 中补充 `proves_fact` 关系的 cardinality 和属性定义 |
| 14 | `README.md` | 项目结构图与实际目录不完全一致 | 新开发者可能困惑 | 同步更新目录结构图，或删除具体文件列表改为高层描述 |

---

## 四、本体论结构专项评估

### 4.1 架构演进评价

v2.0 → v2.2 的演进方向正确，核心改进：
1. **LegalProvisionVersion** 支持法条历史追溯，满足《民法典》替代《侵权责任法》等场景
2. **SentencingStandard** 将量刑/赔偿标准纳入图谱，支撑判决推理概率计算
3. **typically_applies** 关系实现案由→法条映射，解决隐式引用补提问题
4. **domain-specific enums** 按案件类型细分裁判结果，避免 "administrative_decision" 等模糊归类

### 4.2 遗留问题

| 问题 | 当前状态 | 建议 |
|------|----------|------|
| Evidence 实体未在提取流程中落地 | Schema 已定义，Prompt 未要求提取 | 在 `guiding_case_extraction.txt` 中增加证据提取规则 |
| 脱敏引擎仅方案阶段 | `ontology_v2.1_optimization_proposal.md` 中有详细设计 | 优先级提升至 P1，实现 `DesensitizationEngine` 类 |
| 自然人跨案件不关联 | 按设计每个案件独立节点 | 考虑增加 `desensitized_id_number` 前缀匹配实现弱关联 |
| 个体工商户 credit_code 校验过严 | 正则要求 18 位统一代码 | 增加个体工商户注册号格式支持，或改为 soft 校验 |

---

## 五、解析脚本专项评估

### 5.1 文本压缩器（text_compressor.py）

**评分：85/100**

- ✅ 三级压缩策略（短/中/长文本）设计合理
- ✅ 关键结构标记（法院生效裁判认为、争议焦点、综上）覆盖全面
- ✅ 压缩比 23-40%，测试用例（ID 3692/3538）验证有效
- ⚠️ `unnumbered_patterns` 的正则可能误匹配非争议内容（如"关于本案的认定"中的"本案"不是争议焦点）
- ⚠️ 缺少对压缩后信息损失的量化评估指标

### 5.2 法条正则提取器（extract_legal_provisions_regex.py）

**评分：88/100**

- ✅ v2.2 修复致命 typo 后正确率 100%（已提取部分）
- ✅ 5 种模式覆盖完整格式、简写、司法解释、无条号引用
- ✅ 同法多条拆分、HTML 清理、代词排除等细节处理到位
- ⚠️ 模式 C（司法解释）同法多条只能提取第一条
- ⚠️ 模式 B 简写法名最多 10 字符，"涉外民事关系法律适用法"等超长法名可能截断
- ⚠️ 隐式引用（如刑事案由未写《刑法》第 X 条）无法捕获

### 5.3 LLM 提取器（guiding_case_extractor.py / parse_guiding_cases_llm_v2.py）

**评分：78/100**

- ✅ 多提供商支持（OpenAI/DeepSeek/Kimi）
- ✅ 重试机制（指数退避）和 JSON 截断自动修复
- ✅ v2 版集成文本压缩，解决长文本幻觉问题
- ❌ 三个客户端实现几乎相同，代码冗余
- ❌ 错误处理过于粗糙（裸 except Exception）
- ❌ 缺少对 LLM 输出与本体论枚举值的自动校验层

---

## 六、提示词专项评估

### 6.1 提示词版本对比

| 版本 | 文件 | 状态 | 评价 |
|------|------|------|------|
| v1 | `scripts/prompts/guiding_case_extraction.txt` | 已更新 | 角色编码和裁判结果类型已对齐 v2.2 枚举 |
| v2 | `scripts/prompts/guiding_case_ontology_aligned.txt` | 新增 | 更严格的 JSON 格式约束，字段说明更详尽 |
| LLM Extractor | `extraction/llm_extractors/prompt_v1.txt` | 新增 | 与 guiding_case_extraction.txt 内容基本一致 |
| LLM Extractor | `extraction/llm_extractors/prompt_v2.txt` | 新增 | 增加输出示例和容错说明 |

### 6.2 提示词问题

| # | 问题 | 位置 | 建议 |
|---|------|------|------|
| 1 | 提示词中 `case_number` 提取规则过于依赖案号格式正则 | `guiding_case_ontology_aligned.txt:35` | 增加说明：调解/仲裁/司法救助案件可能没有法院案号 |
| 2 | `disputed_issues` 提取要求过于绝对（"必填"） | `guiding_case_ontology_aligned.txt:95` | 增加例外说明：非诉讼案件可无争议焦点 |
| 3 | 缺少对证据信息的提取要求 | `guiding_case_ontology_aligned.txt` | 增加 Evidence 提取规则段落 |
| 4 | 提示词长度超过 3000 tokens | `guiding_case_ontology_aligned.txt` | 考虑拆分为系统提示词 + 少样本示例，减少重复说明 |

---

## 七、可落地的修复方案

### 7.1 本周内（P0 + 部分 P1）

```bash
# 1. 修复硬编码密码
git checkout -b security/fix-hardcoded-credentials
# - docker-compose.yml: 改用 env_file
# - neo4j_hot_client.py: 移除默认密码回退
# - api/routers/query.py: 增加 APIKeyHeader 依赖

# 2. 异常处理细化
git checkout -b refactor/exception-handling
# - parse_guiding_cases_llm_v2.py: 细分异常类型
# - guiding_case_extractor.py: 抽象 LLMClient 工厂
```

### 7.2 两周内（P1）

1. **证据提取落地**
   - 在 `guiding_case_extraction.txt` 中增加 Evidence 提取规则
   - 在 `kg_builder.py` 中补充 `proves_fact` 边构建逻辑
   - 在 20 样本上验证提取效果

2. **案由→法条映射扩展**
   - 将 `case_type_to_provisions.json` 从 17 条扩展至覆盖最高法全部 4 级案由
   - 增加 `applicability_score` 字段，区分主要/次要适用法条

3. **测试覆盖**
   - 为 `text_compressor.py` 添加压缩比和语义保留率测试
   - 为 `extract_legal_provisions_regex.py` 添加边界用例测试（同法多条、超长法名、无书名号）

### 7.3 一个月内（P2 + 架构）

1. **脱敏引擎实现**
   - 实现 `DesensitizationEngine` 类（参考 `ontology_v2.1_optimization_proposal.md`）
   - 增加审计日志实体和抽样验证流程
   - 通过内部 API 隔离调用

2. **API 认证完善**
   - 实现 JWT 认证中间件
   - 增加角色权限（管理员/审核员/只读）
   - 增加请求速率限制

3. **前端连接真实 API**
   - 将 `webui/frontend/app.js` 中的 mock 数据替换为 `/api/v1/*` 调用
   - 增加加载状态和错误提示

---

## 八、结论

2026-04-29 的代码变更体现了项目从**原型验证**向**生产级系统**的关键跃迁：

- **本体论**：v2.2 架构已具备支撑 2 亿+ 裁判文书的理论可行性
- **提取链路**：LLM + 正则 + 案由映射的三层补提策略设计合理
- **评估体系**：20 样本测试 + 根因分析的方法论已建立
- **基础设施**：Neo4j + ES + MinIO + Postgres 的全栈底座已搭建

**主要风险：**
1. 安全基线未达标（硬编码密码、无 API 认证）
2. 测试覆盖率为零，回归风险高
3. LLM 提取在长文本场景仍有 5-10% 的幻觉失败率

**下一步优先事项：**
1. 🔴 修复硬编码密码（P0）
2. 🔴 增加 API 认证（P0）
3. 🟡 补充 pytest 测试套件（P1）
4. 🟡 证据提取规则落地（P1）
5. 🟢 前端连接真实 API（P2）

|---

*本报告由 Hermes Agent「大剑」自动生成，基于对 37 次提交、85 个变更文件的完整分析。*

---

# sxc的魔法老头 | 2026-04-30 08:00

总体评价：评审意见定位精准、分析深入，所列问题几乎全部属实，优先级和修复方案务实可行。仅在正则范围建议上存在微小偏差，整体可作为执行清单直接采纳。

逐条确认：
1. 繁体字“综觀全案”：同意，确属环境不一致，应秒改。
2. 正则汉字范围：部分同意。原`[一-龥]`实际等价于`\u4e00-\u9fa5`，已覆盖基本汉字区；改为`[一-龟]`反而缩窄范围。建议统一为`\u4e00-\u9fa5`更清晰，但优先级可降。
3. `merge_provisions`未定义：同意，属硬缺陷，必须立即补全。
4. Pydantic增加`SentencingStandard`范围约束：同意，用validator实现一致性约束。
5. 案由映射表扩展至50+：同意，短期扩展有价值，长期需考虑自动生成。
6. 争议焦点改用`re.findall`：同意，能捕获多焦点场景，比`re.search`更鲁棒。
7. Prompt-Schema自动校验脚本：同意，可防止枚举不同步，建议CI集成。
8. 双语YAML替代方案：部分同意。当前484行仍可控，可暂用注释但列为技术债务。
9. 压缩完整性校验：同意，缺少信息保真度验证，应加入。
10. 对话轮数限制：同意，防止无限循环，建议设10轮。
11. 中文数字转换单元测试：同意，但需先确认“二百一十条”等是否真有bug，再补测。

遗漏补充：整体缺乏针对关键模块（正则提取、压缩、中文数字转换）的自动化测试用例，即使本次P0修复也需测试保底；建议补建`tests/`目录并接入CI。

后续行动：
- 立即修复：繁体字、`merge_provisions`补全、Pydantic约束。
- 本迭代内：争议焦点多捕获、Prompt一致性校验、案由映射扩展、对话轮数限制。
- 同步建设：为核心提取与压缩逻辑补单元测试，将正则范围写法标准化为`\u4e00-\u9fa5`。

---
